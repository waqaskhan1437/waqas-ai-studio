name: "Video Automation Runner"
run-name: "Video job ${{ inputs.job_id }} ${{ inputs.dispatch_nonce }}"
concurrency:
  group: video-automation-${{ github.run_id }}
  cancel-in-progress: false
on:
  workflow_dispatch:
    inputs:
      job_id:
        description: 'Job ID from worker'
        required: true
        type: string
      automation_id:
        description: 'Automation ID'
        required: true
        type: string
      dispatch_nonce:
        description: 'Unique dispatch nonce used by the worker to correlate this exact GitHub run'
        required: false
        type: string
      worker_webhook_url:
        description: 'Worker webhook URL'
        required: false
        type: string
      runtime_config_token:
        description: 'Short-lived token for fetching runtime config from worker'
        required: false
        type: string
      runner_labels:
        description: 'JSON array of runner labels'
        required: false
        type: string

permissions:
  contents: write
  actions: read

jobs:
  process-video:
    runs-on: ${{ fromJson(github.event.inputs.runner_labels || '["ubuntu-latest"]') }}
    timeout-minutes: 360

    env:
      WORKER_WEBHOOK_URL: ${{ github.event.inputs.worker_webhook_url }}
      JOB_ID: ${{ github.event.inputs.job_id }}
      AUTOMATION_ID: ${{ github.event.inputs.automation_id }}
      RUNNER_EXECUTION_MODE: github
      RUNTIME_CONFIG_TOKEN: ${{ github.event.inputs.runtime_config_token }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup runner Node workspace
        uses: ./.github/actions/setup-runner-node

      - name: Save config
        run: node runner-scripts/fetch-runtime-config.js

      - name: Install & update yt-dlp (latest)
        timeout-minutes: 8
        run: |
          set -euo pipefail
          # Upgrade yt-dlp to latest version every run (critical for YouTube 2026 compatibility)
          python3 -m pip install -q "yt-dlp[default]" --upgrade --break-system-packages
          YT_VERSION="$(yt-dlp --version 2>&1)"
          echo "yt-dlp version: $YT_VERSION"
          # Verify extractors are functional
          yt-dlp --list-extractors 2>&1 | grep -qi youtube || { echo "YouTube extractor check failed (yt-dlp $YT_VERSION)"; exit 1; }
          # Check JS runtime support (required for n-challenge solving)
          if yt-dlp --help 2>&1 | grep -q "js-runtimes"; then
            echo "js-runtimes: available"
          else
            echo "FATAL: yt-dlp $YT_VERSION does not support --js-runtimes (required for YouTube 2026)"; exit 1
          fi
          # Check node is available (JS runtime for n-challenge)
          which node && echo "Node.js: $(node --version)" || { echo "FATAL: node not found (required for JS runtime)"; exit 1; }
          # Check if yt-dlp-ejs is bundled (from yt-dlp[default] pip package)
          EJS_BUNDLED=false
          if python3 -c "import yt_dlp_ejs" 2>/dev/null; then
            echo "yt-dlp-ejs: installed (offline — EJS solver bundled)"
            EJS_BUNDLED=true
          else
            echo "yt-dlp-ejs: not bundled (will use --remote-components)"
          fi
          # Pre-download EJS scripts for n-challenge solving (if not bundled)
          if [ "$EJS_BUNDLED" = false ] && yt-dlp --help 2>&1 | grep -q "remote-components"; then
            echo "Pre-downloading EJS components from GitHub..."
            yt-dlp --remote-components ejs:github -v 2>&1 || echo "WARNING: EJS pre-download failed — will retry at download time"
          elif [ "$EJS_BUNDLED" = false ]; then
            echo "WARNING: No EJS solver available (neither bundled nor --remote-components). YouTube n-challenge may fail."
          else
            echo "EJS solver: bundled (no pre-download needed)"
          fi
          echo "yt-dlp setup complete"
          echo ""

      - name: Install FFmpeg and fonts
        timeout-minutes: 8
        run: |
          set -euo pipefail
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            ca-certificates \
            curl \
            ffmpeg \
            fonts-dejavu-core \
            fonts-ubuntu \
            python3
          mkdir -p /usr/share/fonts/truetype/noto
          if [ -f runner-scripts/fonts/NotoNastaliqUrdu.ttf ]; then
            cp runner-scripts/fonts/NotoNastaliqUrdu.ttf /usr/share/fonts/truetype/noto/NotoNastaliqUrdu.ttf
          fi
          fc-cache -f -v >/dev/null 2>&1 || true
          fc-match "Noto Nastaliq Urdu" || true
          ffmpeg -version | head -1

      - name: Install Playwright Chromium
        timeout-minutes: 12
        run: |
          set -euo pipefail
          cd runner-scripts
          npx --no-install playwright-core install --with-deps chromium
          BROWSER_PATH="$(node -e 'const { chromium } = require("playwright-core"); console.log(chromium.executablePath())')"
          test -x "$BROWSER_PATH"
          echo "PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=$BROWSER_PATH" >> "$GITHUB_ENV"
          node - <<'NODE'
          const { chromium } = require('playwright-core');
          (async () => {
            const executablePath = process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH || chromium.executablePath();
            const browser = await chromium.launch({ executablePath, headless: true, args: ['--no-sandbox', '--disable-dev-shm-usage'] });
            const page = await browser.newPage();
            await page.setContent('<html><body>ok</body></html>');
            await browser.close();
            console.log(`Chromium smoke test OK: ${executablePath}`);
          })().catch((error) => {
            console.error(error);
            process.exit(1);
          });
          NODE

      - name: Install Whisper (if captions enabled)
        timeout-minutes: 10
        run: |
          cd runner-scripts
          WHISPER=$(node -e "try { const c = require('./automation-config.json'); process.exit(c.whisper_enabled === true ? 0 : 1); } catch { process.exit(1); }" 2>/dev/null && echo "install" || echo "skip")
          if [ "$WHISPER" = "install" ]; then
            echo "Installing openai-whisper (for captions)..."
            pip install -q openai-whisper --upgrade
            echo "Whisper installed"
          else
            echo "Captions disabled — skipping whisper install"
          fi

      - name: Download and Process Videos
        timeout-minutes: 330
        run: |
          cd runner-scripts
          mkdir -p output
          
          VIDEOS_PER_RUN=$(node -e "console.log(require('./automation-config.json').videos_per_run || 1)")
          TOTAL_URLS=$(node -e "console.log(require('./automation-config.json').video_urls?.length || 0)")
          
          echo "=============================================="
          echo "Videos per run: $VIDEOS_PER_RUN"
          echo "Total URLs: $TOTAL_URLS"
          echo "=============================================="
          
          set +e
          node main.js 2>&1 | tee output/runner.log
          MAIN_EXIT_CODE=${PIPESTATUS[0]}
          set -e

          if [ "$MAIN_EXIT_CODE" -ne 0 ]; then
            echo "main.js exited with code $MAIN_EXIT_CODE"
            {
              echo "=== runner.log tail ==="
              tail -n 300 output/runner.log 2>/dev/null || true
              echo "=== error.log ==="
              cat output/error.log 2>/dev/null || true
            } | tee -a output/error.log
          fi

          echo "=============================================="
          ls -la output/ 2>/dev/null || echo "No output"
          echo "=============================================="

          exit "$MAIN_EXIT_CODE"

      - name: Update Status on Failure
        if: failure()
        timeout-minutes: 2
        run: |
          cd runner-scripts
          if [ -f output/error.log ]; then
            echo "=== Error Log ==="
            cat output/error.log
          fi
          node update-job-status.js || true

      - name: Upload artifacts
        if: always()
        uses: ./.github/actions/upload-runner-artifacts
        with:
          name: job-debug-${{ github.event.inputs.job_id }}
          path: runner-scripts/output/
